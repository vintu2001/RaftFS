raft:
  node-id: ${NODE_ID:node-0}
  peers: ${PEERS:localhost:8081}
  port: ${RAFT_PORT:8081}

storage:
  data-dir: ${DATA_DIR:/data}
  local-address: ${LOCAL_ADDRESS:localhost:8080}
  gcs:
    bucket: ${GCS_BUCKET:}
    project-id: ${GCS_PROJECT_ID:}

grpc:
  port: ${GRPC_PORT:8080}

logging:
  level:
    com.raftfs: INFO
